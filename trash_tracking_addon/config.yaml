# Home Assistant Add-on Configuration
name: "Trash Tracking"
version: "1.0.0"
slug: trash_tracking
description: "Real-time trash truck tracking and automation system for New Taipei City"
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
init: false
startup: application
boot: auto
map:
  - config:rw
ports:
  5000/tcp: 5000
ports_description:
  5000/tcp: "API service port"
options:
  location:
    lat: 25.018269
    lng: 121.471703
  tracking:
    target_lines: []
    enter_point: "Minsheng Rd. Sec. 2, No. 80"
    exit_point: "Chenggong Rd. No. 23"
    trigger_mode: "arriving"
    approaching_threshold: 2
  system:
    log_level: "INFO"
  api:
    ntpc:
      timeout: 10
      retry_count: 3
      retry_delay: 2
schema:
  location:
    lat: float
    lng: float
  tracking:
    target_lines:
      - str?
    enter_point: str
    exit_point: str
    trigger_mode: list(arriving|arrived)
    approaching_threshold: int(0,10)
  system:
    log_level: list(DEBUG|INFO|WARNING|ERROR)
  api:
    ntpc:
      timeout: int(5,30)
      retry_count: int(1,10)
      retry_delay: int(1,10)
image: "ghcr.io/{arch}-trash-tracking"
